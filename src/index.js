import React from "react";
import ReactDOM from "react-dom";
import { Starter } from "./app/starter";

const starter = new Starter(React, ReactDOM);

window.starter = starter;

export default starter;
